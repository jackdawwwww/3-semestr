#ifndef GAMEMODE_H_
#define GAMEMODE_H_

enum class GameMode { GAME_IN_PROCESS, GAMER_WIN, ENEMY_WIN };
enum class GamerMode { CONSOLE, RANDOM, SMART };
enum class ShipsTypes {SINGLE, TWO, THREE, FOUR};

#endif //GAMEMODE_H_