#ifndef GAMESETTINGS_H
#define GAMESETTINGS_H

#include <iostream>
#include <map>
#include <string>

using namespace std;

const size_t SIZE = 10;
const size_t FOUR_DESK = 1;
const size_t THREE_DESK = 2;
const size_t TWO_DESK = 3;
const size_t SINGE_DESK = 4;

#endif //GAMESETTINGS_H